<body>
    <div class="container">
        <h3>Elige un espacio a asociar con el usuario</h3>    
        <table class="table">
            <tr>
                <th>Nombre</th>
                <th>Descripción</th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
                <% _.each(locations, function(location) { %>
            <tr data-id="<%= location.id %>" data-model="location">
                <td><%= location.name %></td>
                <td><%= location.description %></td>
                <td>
                    <form action="/user/associateLocationToUser/" method="POST" >                    
                        <input type="hidden" name="location_id" value="<%= location.id %>" />
                        <input type="hidden" name="user_id" value="<%= user_id %>" />
                        <input type="submit" value="Asociar" class="btn btn-sm"/>
                        <input type="hidden" name="_csrf" value="<%= _csrf %>" />
                    </form>
                </td>
            </tr>
                <% }) %>
        </table>
        <br/>
    </div>
</body>